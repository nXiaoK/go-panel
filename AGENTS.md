# 项目协作规则

## Git 提交

- 当一次会话已经完整解决用户提出的问题，并完成与改动风险相匹配的验证后，必须提交本次相关的 Git 更改。
- 提交前必须检查工作区，只提交本次会话相关内容，不得混入用户已有或其他任务的改动。
- 如果问题尚未完整解决、验证失败或仍在等待用户决定，不得为了满足流程而提交不完整结果。

## 配置中文注释

- 所有新增或修改的配置文件、配置模板、配置示例、配置项以及配置相关代码，都必须增加清晰的中文注释，说明用途、默认行为、关键取值和可能的风险。
- 注释应贴近对应配置，不能只写笼统标题；涉及网络、安全、权限、数据迁移或兼容性的配置必须明确说明影响。
- 对严格 JSON 等语法本身不允许注释的格式，不得插入会导致配置失效的伪注释；必须在相邻的中文 Markdown 文档中按配置模块补充说明，并在文档与配置之间建立明确引用。

## 任务委派

- 主会话判断任务需要委派或并行处理时，必须创建独立的 Luna Max 线程：模型固定使用 `gpt-5.6-luna`，推理强度固定使用 `max`。
- 主会话不得直接创建或调用子 Agent；无需委派的任务由主会话直接完成，不得为满足形式要求强制创建线程。
- Luna Max 线程可以根据任务需要自行创建和协调子代理，但仍须遵守本文件的 Git 提交、配置中文注释及其他项目约束。
